{
  "name": "qcluster",
  "version": "0.8.0",
  "description": "robust worker cluster management",
  "main": "qcluster.js",
  "license": "Apache-2.0",
  "keywords": [ "robust", "cluster", "Andras", "race-free", "deterministic" ],
  "repository": "https://github.com/andrasq/node-qcluster",
  "dependencies": {
  },
  "devDependencies": {
    "qnit": "0.18.0"
  },
  "scripts": {
    "test": "qnit -t 4000 test-*",
    "coverage": "nyc --include qcluster.js --reporter text --reporter lcov qnit --timeout 10000 test-*",
    "clean": "rm -rf .nyc_output coverage"
  }
}
